apiVersion: v1
kind: Service
metadata:
  name: rust-api-svc
  namespace: mumk8s
  labels:
    app: rust-api
spec:
  selector:
    app: rust-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
