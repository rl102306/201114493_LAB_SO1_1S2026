apiVersion: v1
kind: Service
metadata:
  name: go-rabbitmq-writer-svc
  namespace: mumk8s
  labels:
    app: go-rabbitmq-writer
spec:
  selector:
    app: go-rabbitmq-writer
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
